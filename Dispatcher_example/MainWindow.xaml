<Window x:Class="Dispatcher_example.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dispatcher_example"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button x:Name="startNotepad_button"
                Click="StartNotepad_Click"
                Content="Запустить Notepad"
                FontSize="20"
                Grid.Column="0"
                Grid.Row="0"
                Margin="50"/>

        <Button x:Name="reloadTextBlock_button"
                Click="ReloadTextBlock_Click"
                Content="Обновить текстовое поле"
                FontSize="20"
                Grid.Column="1"
                Grid.Row="0"
                Margin="50"/>

        <TextBlock x:Name="stateTextBlock"
                   Text="{Binding }"
                   FontSize="20"
                   Grid.ColumnSpan="2"
                   Grid.Row="1"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

    </Grid>
</Window>
